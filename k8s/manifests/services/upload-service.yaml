---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: upload-service
  namespace: arch-analyzer
  labels:
    app: upload-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: upload-service
  template:
    metadata:
      labels:
        app: upload-service
    spec:
      containers:
      - name: upload-service
        image: arch-analyzer/upload-service:local
        imagePullPolicy: Never
        ports:
        - containerPort: 8001
        env:
        - name: DATABASE_URL
          value: "postgresql+asyncpg://fiap:fiap@postgres:5432/fiap"
        - name: RABBITMQ_HOST
          value: "rabbitmq"
        - name: RABBITMQ_PORT
          value: "5672"
        - name: RABBITMQ_USER
          valueFrom:
            secretKeyRef:
              name: arch-analyzer-secrets
              key: RABBITMQ_USER
        - name: RABBITMQ_PASSWORD
          valueFrom:
            secretKeyRef:
              name: arch-analyzer-secrets
              key: RABBITMQ_PASSWORD
        - name: MINIO_ENDPOINT
          value: "minio:9000"
        - name: MINIO_ACCESS_KEY
          valueFrom:
            secretKeyRef:
              name: arch-analyzer-secrets
              key: MINIO_ACCESS_KEY
        - name: MINIO_SECRET_KEY
          valueFrom:
            secretKeyRef:
              name: arch-analyzer-secrets
              key: MINIO_SECRET_KEY
        - name: MINIO_BUCKET
          value: "fiap"
        - name: MINIO_REGION
          value: "us-east-1"
        - name: MINIO_USE_SSL
          value: "false"
        - name: SERVICE_NAME
          value: "upload-service"
        readinessProbe:
          httpGet:
            path: /health
            port: 8001
          initialDelaySeconds: 15
          periodSeconds: 10
          failureThreshold: 5
        resources:
          requests:
            memory: "256Mi"
            cpu: "100m"
          limits:
            memory: "512Mi"
            cpu: "500m"
---
apiVersion: v1
kind: Service
metadata:
  name: upload-service
  namespace: arch-analyzer
spec:
  selector:
    app: upload-service
  ports:
  - port: 8001
    targetPort: 8001
