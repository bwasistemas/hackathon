# Copie este arquivo para 02-secrets.yaml e preencha OPENAI_API_KEY
# com o valor real antes de rodar o k8s-setup.sh
apiVersion: v1
kind: Secret
metadata:
  name: arch-analyzer-secrets
  namespace: arch-analyzer
type: Opaque
stringData:
  POSTGRES_USER: "fiap"
  POSTGRES_PASSWORD: "fiap"
  POSTGRES_DB: "fiap"
  RABBITMQ_USER: "fiap"
  RABBITMQ_PASSWORD: "fiap"
  MINIO_ACCESS_KEY: "fiap"
  MINIO_SECRET_KEY: "fiap1234"
  GRAFANA_USER: "fiap"
  GRAFANA_PASSWORD: "fiap"
  # Preencha com sua chave real
  OPENAI_API_KEY: ""
  OPENAI_BASE_URL: "https://api.openai.com/v1"
  LLM_MODEL: "gpt-4o-mini"
  LLM_OCR: ""
