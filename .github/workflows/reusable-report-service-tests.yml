# Reusable test job for report-service. Call from PR workflows, workflow_dispatch, or deploy pipelines.
name: Reusable — Report Service Tests

on:
  workflow_call:

permissions:
  contents: read

jobs:
  test-report-service:
    name: pytest (services/report-service)
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip
          cache-dependency-path: |
            services/report-service/requirements.txt
            services/report-service/requirements-dev.txt

      - name: Install dependencies
        working-directory: services/report-service
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt -r requirements-dev.txt

      - name: Run tests
        working-directory: services/report-service
        run: |
          pytest -q
